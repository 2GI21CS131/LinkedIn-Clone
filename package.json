{
  "name": "linkedin-clone",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
  "test": "echo \"No tests configured\" && exit 0",
    "bootstrap": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm --workspace backend run start\" \"npm --workspace frontend run start\""
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
